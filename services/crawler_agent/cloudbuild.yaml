steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - services/crawler_agent/Dockerfile
      - --tag
      - ${_IMAGE}
      - .
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_IMAGE}
images:
  - ${_IMAGE}
